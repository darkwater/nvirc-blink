<settings>
    <section label="General" icon="gear">
        <fieldset label="Daemon settings" layout="list">
            <p>Lorem ipsum.</p>
            <string label="Hostname" path="local.daemon.host" example="localhost" />
            <number label="Port" path="local.daemon.port" example="3333" />
            <string label="Password" path="local.daemon.password" masked="true" />
        </fieldset>
    </section>
    <section label="Notificatons" icon="comment-o">
        <fieldset label="Hello world" layout="list">
            <p>Dolor, sit amet.</p>
        </fieldset>
    </section>
</settings>
